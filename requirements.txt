# Cloud (Hugging Face Spaces / Streamlit Cloud) dependencies.
# Playwright is intentionally OMITTED — scraping only runs on the local desktop
# install. The app imports it lazily, so the cloud build stays fast + reliable.

streamlit==1.57.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
anthropic>=0.40.0
pytrends>=4.9.0
openpyxl>=3.1.0
reportlab>=4.0.0
Pillow>=10.0.0
